POSTGRES_USER=changeUser
POSTGRES_PASSWORD=changePassword
POSTGRES_DB=n8n

POSTGRES_NON_ROOT_USER=changeUser
POSTGRES_NON_ROOT_PASSWORD=changePassword
FLAME_PASSWORD=changeMe
REDIS_PASSWORD=changeMe
REDIS_PUBSUB_PASSWORD=changeMe
RI_ENCRYPTION_KEY=changeMeToAStrongRandomKey
UMAMI_APP_SECRET=replace-me-with-a-random-string
METABASE_DB_PASSWORD=changeMe
TZ=Etc/UTC
PIHOLE_PASSWORD=changeme
DB_DATA_LOCATION=/Users/logan/docker-volumes/immich/immich/postgres
BACKREST_USERDATA=/Users/logan/docker-volumes
# BACKREST_REPOS=/Users/logan/repos
TA_HOST=http://tubearchivist.homelab
TA_USERNAME=tubearchivist
TA_PASSWORD=changeMe
TA_ELASTIC_PASSWORD=changeMe
MATOMO_DB_ROOT_PASSWORD=changeMe
MATOMO_DB_PASSWORD=changeMe
ANTHROPIC_API_KEY=sk-ant-changeMe
HERMES_DASHBOARD_USER=admin
HERMES_DASHBOARD_PASSWORD=changeMe
HERMES_DASHBOARD_SECRET=changeMeToARandomHexString
ARCHIVEBOX_ADMIN_PASSWORD=changeme

# Tailscale: reusable auth key from login.tailscale.com -> Settings -> Keys
TS_AUTHKEY=changeMe
# Beszel: both values come from the hub UI (port 8090) -> Add System / Settings -> Tokens
BESZEL_KEY=changeMe
BESZEL_TOKEN=changeMe
# Jellyfin: host path to the music library, mounted read-only at /media/music.
# Must exist and be mounted before the container is created, or the library is empty.
JELLYFIN_MUSIC_DIR=/mnt/ssd/music
JELLYFIN_MOVIES_DIR=/mnt/ssd/movies

# Penpot config lives in docker/penpot/.env
# Navidrome config lives in docker/navidrome/.env
